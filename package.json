{
  "name": "task-management",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev:frontend": "bun --cwd frontend dev",
    "dev:backend": "bun --cwd backend dev",
    "typecheck": "bun --cwd frontend run typecheck && bun --cwd backend run typecheck"
  }
}
